# json.set! @messages, :id, :content, :from, :timestamp, :created_at, :updated_at,
# @message = json.messages Message.all, :content, :from, :timestamp
# json.data(@data) { |d| json.extract!(d, :content, :from, :timestamp) }
